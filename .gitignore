# Dependencies
node_modules/
vendor/

# Build output (regenerated by `npm run build`)
# ⛔ BOTH halves. assets/admin/ is the Vue app; assets/*.js are the three
# standalone front-end scripts, minified out of resources/public/ by esbuild —
# their SOURCE is what the repo keeps, so a clone always needs a build.
assets/admin/
assets/*.js

# Local dev tooling / AI session state
dev/

# OS / editors
.DS_Store
*.log
.idea/
.vscode/
.preview.html
